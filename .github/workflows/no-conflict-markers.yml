name: no-conflict-markers

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Scan tracked files for unresolved merge conflict markers
        run: bash scripts/check-conflict-markers.sh
